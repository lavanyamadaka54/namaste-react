***React provides utility functions
like hooks
useState = it is one of the react hooks
           it will return array of state variable and a method to uodate that state variable
           as sson as state variable updates react will re-renders/refresh/updatesui the componets

react good at DOM manipulations hoe react will findout the diff b/w the prev and current virtual doms(object representation of real dom) and updated the UI(original dom)    