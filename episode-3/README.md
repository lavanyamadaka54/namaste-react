1. npm start = npm run start = behind the scenes it will execute the 'parcel index.html'(why because we have added in scripts)
2. Babel will convert JSX to React
  - if you had to give attribites to jsx give those in camelCase